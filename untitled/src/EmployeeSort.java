import java.time.LocalDate;
import java.util.Arrays;
import java.util.List;

public class EmployeeSort {

    public static void main(){

        List<Employee> employees = Arrays.asList(new Employee(1L,10000, new LocalDate()))

                employees.stream().filter(e ->e.getSalary()>10000).sorted((e1, e2) ->{
                   if (e1.getSalary() >e1.getSalary(){
                       return 1;
                    }
                    else if (e1.getSalary() >e1.getSalary(){
                        return -1;
                    }
                    else{
                        e1.getDateOfJoining() >
                    }
                }
    }
}
